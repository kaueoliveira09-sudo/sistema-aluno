const nomeAluno = "João Silva"; // Variável String
const idade = 21;              // Variável Number
const matriculado = true;       // Variável Boolean

// Este é o console detalhado da branch feature/mensagem-detalhada
console.log(`Relatório do Sistema: O aluno ${nomeAluno} (Idade: ${idade}) está atualmente ${matriculado ? "Ativo" : "Inativo"} no sistema.`);